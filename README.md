# Front-Andreev Blog

Тестовое задание React-приложения для отображения списка постов с поиском и модальным окном.

## 🚀 Технологии

- **React 19** - библиотека для создания пользовательских интерфейсов
- **TypeScript** - строгая типизация для повышения качества кода
- **Vite** - быстрый сборщик модулей
- **CSS3** - стилизация с использованием CSS Variables и Flexbox

## 📦 Установка

```bash
npm install
```

## 🛠️ Разработка

Запуск dev сервера:

```bash
npm run dev
```

Приложение откроется по адресу: http://localhost:5173

## 🏗️ Сборка

Production сборка:

```bash
npm run build
```

Предпросмотр production сборки:

```bash
npm run preview
```

## 📋 Функционал

- ✅ Отображение списка постов из API
- ✅ Поиск по заголовкам и описаниям
- ✅ Модальное окно с полной информацией о посте
- ✅ Адаптивный дизайн (desktop/tablet/mobile)
- ✅ Sticky меню с плавным скрытием/показом
- ✅ Мобильное меню с выдвижением слева
- ✅ Retina изображения (srcset)
- ✅ Скелетоны загрузки
- ✅ Обработка ошибок
- ✅ Accessibility (WCAG)

## 📁 Структура проекта

```
src/
├── components/          # React компоненты
│   ├── Header/         # Шапка сайта и навигация
│   ├── Icons/          # SVG иконки
│   ├── Modal/          # Модальное окно
│   ├── PostGrid/       # Сетка постов
│   ├── SearchBar/      # Строка поиска
│   └── common/         # Общие компоненты
├── hooks/              # Custom React hooks
│   ├── usePosts.ts     # Загрузка постов
│   └── useSearch.ts    # Поиск по постам
├── services/           # API сервисы
│   └── api.ts          # HTTP клиент
├── types.ts            # TypeScript типы
├── App.tsx             # Главный компонент
└── main.tsx            # Точка входа

```

## 🎨 Особенности реализации

### TypeScript

- Строгая типизация всех компонентов и функций
- Интерфейсы для Props
- Generic типы для API
- Type guards для безопасности

### Производительность

- Code splitting (React.lazy)
- Мемоизация (useCallback, useMemo)
- Lazy loading изображений
- Оптимизация bundle size

### Доступность

- ARIA атрибуты
- Keyboard navigation
- Focus trap в модальном окне
- Семантичная HTML разметка

### Безопасность

- DOMPurify для санитизации HTML
- XSS защита
- CSP headers готовность

## 🌐 API

Данные загружаются из:

```
https://cloud.codesupply.co/endpoint/react/data.json
```

## 📱 Адаптивность

- **Desktop**: 1024px и выше (3 колонки)
- **Tablet**: 768px - 1023px (2 колонки)
- **Mobile**: до 767px (1 колонка)

## 🎯 Требования к ТЗ

✅ Все требования выполнены:

- Верстка по Figma макету
- Sticky меню с логикой скрытия
- Мобильное меню
- Подменю на CSS
- Google Fonts (Roboto)
- Размеры в rem (кроме gap в px)
- Retina изображения
- Flex сетка
- React Hooks (useState, useEffect, useCallback, useMemo)
- Fetch API
- Поиск по постам
- Модальное окно

## 📄 Лицензия

MIT
