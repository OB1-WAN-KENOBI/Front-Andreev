# Front-Andreev

Современное адаптивное веб-приложение для отображения постов с поиском, модальными окнами и мобильным меню.

## 🚀 Демо

[https://ob1-wan-kenobi.github.io/Front-Andreev/](https://ob1-wan-kenobi.github.io/Front-Andreev/)

## 📋 Описание

React-приложение, созданное на основе дизайна Figma, с полной адаптивностью для desktop, tablet и mobile устройств. Приложение загружает список постов из внешнего API и предоставляет удобный интерфейс для их просмотра и поиска.

## ✨ Основные возможности

- 📱 **Полная адаптивность**: Desktop (3 колонки), Tablet (2 колонки), Mobile (1 колонка)
- 🔍 **Поиск в реальном времени** по заголовкам и описаниям постов
- 🖼️ **Retina поддержка** изображений с использованием `srcset`
- 🎯 **Модальные окна** для детального просмотра постов
- 🎨 **Sticky-навигация** с анимацией показа/скрытия при скролле
- 📲 **Мобильное меню** с плавной анимацией и подменю
- ♿ **Доступность**: keyboard navigation, focus traps, ARIA-атрибуты
- ⚡ **Оптимизация производительности**: lazy loading, мемоизация, code splitting
- 🔒 **Безопасность**: защита от XSS с помощью DOMPurify
- 🎭 **Skeleton loaders** для улучшения UX при загрузке

## 🛠️ Технологии

### Core

- **React** 19.2.0
- **TypeScript** 5.9.3
- **Vite** 7.2.4

### UI & Styling

- **CSS Grid** для адаптивной сетки
- **CSS Custom Properties** для единообразного дизайна
- **Google Fonts** (Roboto)

### Дополнительные библиотеки

- **DOMPurify** — защита от XSS
- **focus-trap-react** — управление фокусом в модальных окнах

### Инструменты разработки

- **ESLint** с TypeScript плагинами
- **gh-pages** для деплоя
- **Git** для контроля версий

## 📁 Структура проекта

```
src/
├── components/
│   ├── common/
│   │   ├── LoadingSkeleton.tsx    # Скелетон для загрузки
│   │   └── LoadingSkeleton.css
│   ├── Header/
│   │   ├── Header.tsx             # Шапка сайта
│   │   ├── Navigation.tsx         # Desktop навигация
│   │   ├── MobileMenu.tsx         # Мобильное меню
│   │   └── *.css
│   ├── Icons/
│   │   ├── ArrowIcon.tsx          # SVG иконка стрелки
│   │   ├── LogotypeIcon.tsx       # SVG логотип
│   │   └── SearchIcon.tsx         # SVG иконка поиска
│   ├── Modal/
│   │   ├── Modal.tsx              # Модальное окно
│   │   └── Modal.css
│   ├── PostGrid/
│   │   ├── PostCard.tsx           # Карточка поста
│   │   ├── PostGrid.tsx           # Сетка постов
│   │   └── *.css
│   └── SearchBar/
│       ├── SearchBar.tsx          # Строка поиска
│       └── SearchBar.css
├── hooks/
│   ├── usePosts.ts                # Хук для загрузки постов
│   └── useSearch.ts               # Хук для поиска
├── services/
│   └── api.ts                     # API слой для запросов
├── types.ts                       # TypeScript типы
├── App.tsx                        # Главный компонент
├── main.tsx                       # Точка входа
└── index.css                      # Глобальные стили
```

## 🚀 Быстрый старт

### Установка

```bash
# Клонировать репозиторий
git clone https://github.com/OB1-WAN-KENOBI/Front-Andreev.git

# Перейти в директорию
cd Front-Andreev

# Установить зависимости
npm install
```

### Разработка

```bash
# Запустить dev-сервер
npm run dev
```

Приложение будет доступно по адресу: `http://localhost:5173/`

### Сборка

```bash
# Создать production сборку
npm run build

# Предпросмотр production сборки
npm run preview
```

### Деплой

```bash
# Задеплоить на GitHub Pages
npm run deploy
```

### Линтинг

```bash
# Проверить код
npm run lint
```

## 🎨 Особенности реализации

### Адаптивная сетка (CSS Grid)

```css
.post-grid {
  display: grid;
  grid-template-columns: repeat(3, 1fr); /* Desktop: 3 колонки */
  gap: 2.5rem;
}

@media (max-width: 1023px) {
  grid-template-columns: repeat(2, 1fr); /* Tablet: 2 колонки */
}

@media (max-width: 767px) {
  grid-template-columns: 1fr; /* Mobile: 1 колонка */
}
```

### Sticky-навигация с умным скрытием

Навигация скрывается при скролле вниз и появляется при скролле вверх после 200px.

### Retina изображения

```tsx
<img
  src={post.img}
  srcSet={`${post.img} 1x, ${post.img_2x} 2x`}
  alt={post.title}
  loading="lazy"
/>
```

### Оптимизация производительности

- Lazy loading компонентов (Modal, SearchBar)
- Мемоизация обработчиков событий с `useCallback`
- Debouncing для поиска
- Retry логика для API запросов

### Доступность

- Семантическая HTML разметка
- Keyboard navigation (Enter, Space, Escape)
- ARIA атрибуты (`role`, `aria-label`, `aria-expanded`)
- Focus trap в модальных окнах
- Focus-visible стили

## 🔧 Настройка

### Изменение API источника

Измените URL в `src/services/api.ts`:

```typescript
const API_BASE_URL = "https://your-api.com/endpoint";
```

### Настройка цветов

Цвета настраиваются через CSS переменные в `src/index.css`:

```css
:root {
  --color-accent: #eb0028;
  --color-text: #000000;
  --color-background: #ffffff;
}
```

## 📊 Браузерная совместимость

- Chrome (последние 2 версии)
- Firefox (последние 2 версии)
- Safari (последние 2 версии)
- Edge (последние 2 версии)

## 📝 Архитектурные решения

### Почему CSS Grid вместо Flexbox?

- Проще управлять двумерной сеткой
- Меньше вычислений с `calc()`
- Более предсказуемое поведение на разных браузерах

### Почему TypeScript?

- Статическая типизация
- Лучший DX с автодополнением
- Меньше ошибок во время выполнения

### Почему кастомные хуки?

- Переиспользуемая логика
- Разделение concerns
- Упрощение тестирования

## 🤝 Контрибьютинг

1. Fork проекта
2. Создайте feature branch (`git checkout -b feature/AmazingFeature`)
3. Commit изменения (`git commit -m 'Add some AmazingFeature'`)
4. Push в branch (`git push origin feature/AmazingFeature`)
5. Откройте Pull Request

## 📄 Лицензия

Этот проект является учебным и создан в образовательных целях.

## 👤 Автор

**OB1-WAN-KENOBI**

- GitHub: [@OB1-WAN-KENOBI](https://github.com/OB1-WAN-KENOBI)
- Project Link: [https://github.com/OB1-WAN-KENOBI/Front-Andreev](https://github.com/OB1-WAN-KENOBI/Front-Andreev)

## 🙏 Благодарности

- Дизайн: [Figma Design](https://www.figma.com/design/uasnqjS5ltwuYnuGWVCx6Y/Тестовое)
- API: [CodeSupply](https://cloud.codesupply.co/endpoint/react/data.json)
- Иконки: Custom SVG
- Шрифты: [Google Fonts (Roboto)](https://fonts.google.com/specimen/Roboto)
